/**
 * Admin PIN is verified only on the server (admin_auth_api.php).
 * Use verifyAdminPin() from src/api/adminAuth.ts — never hardcode the PIN here.
 */
export const ADMIN_SESSION_HINT = 'Use Daratech PIN at /daratech (verified server-side).';
